name: Rollback

on:
  workflow_dispatch:
    inputs:
      sha:
        description: 'Full git SHA of the image to roll back to'
        required: true
        type: string

jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - name: Tailscale
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:deploy

      - uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd __SERVER_DIR__
            export APP_IMAGE_TAG=${{ inputs.sha }}
            echo "$APP_IMAGE_TAG" > .app_image_tag
            docker compose -f docker/compose.prod.yaml pull app
            docker compose -f docker/compose.prod.yaml up -d__COMPOSE_UP_SCALE__
